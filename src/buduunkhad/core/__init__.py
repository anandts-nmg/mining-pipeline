"""Core primitives that enforce the methodology's governing invariants.

These modules are deliberately small and well-tested so the invariants (raw
read-only, sidecar bundling, EPSG:32647, versioned naming, QA/QC + gates) are hard
to violate from phase code.
"""
