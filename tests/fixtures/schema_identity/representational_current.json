{
  "$defs": {
    "CurrentGeneratedName": {
      "additionalProperties": false,
      "description": "Current framework wording.",
      "properties": {
        "kind": {
          "const": "alpha",
          "default": "alpha",
          "title": "Kind",
          "type": "string"
        },
        "value": {
          "description": "A value.",
          "minLength": 1,
          "title": "Value",
          "type": "string"
        }
      },
      "required": ["kind", "value"],
      "title": "CurrentGeneratedName",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "item": {"$ref": "#/$defs/CurrentGeneratedName"},
    "optional": {
      "anyOf": [{"type": "integer"}, {"type": "null"}],
      "default": null,
      "title": "Optional"
    }
  },
  "required": ["item", "optional"],
  "title": "CurrentRoot",
  "type": "object"
}
